One-line: open-arc gauge used for the workout timer, sleep score and device pairing.

```jsx
<ProgressDial value={70} label="22.5" sublabel="Sec" sweep={300} />
```

- Arc is Blue Glow on a white-10% track with round caps; the readout inside is always Doto.
