One-line: circular single-glyph control — back arrows, share, bell, close, add.

```jsx
<IconButton icon="chevron-left" label="Back" />
<IconButton icon="share" tone="glass" size={40} />
```

- Always perfectly round. `dark` (#292929) is the default on black; `glass` when it floats over photography.
