One-line: chart key — a 9px square swatch plus an 11px Inter label.

```jsx
<Legend items={[{label:"Calories Burned",color:"var(--data-calories)"},{label:"Nutrition",color:"var(--data-nutrition)"}]} />
```
