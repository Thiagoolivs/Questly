fastapi==0.111.0
uvicorn[standard]==0.30.1
SQLAlchemy==2.0.31
pydantic==2.8.2
# Tokens JWT (puro Python, sem dependência nativa).
PyJWT==2.9.0
# Web Push (notificações). Puxa cryptography (com wheels) e py-vapid/http-ece.
pywebpush==2.3.0
# Necessário apenas quando DATABASE_URL (Postgres) estiver definido — ex: Railway.
psycopg[binary]==3.2.1
# IA (desafios + contador de calorias). Opcionais: usados se a chave existir.
groq==0.13.0
openai>=1.40,<2
